export const selectedImage = state => {
    return state.image.selected_image
}

export const processedImage = state => {
    return state.image.processed_image
}

export const imageProcesses = state => {
    return state.image.image_processes
}
