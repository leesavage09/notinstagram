require "rails_helper"

RSpec.describe Notification, type: :model do

  it "can create a valid notification." do
    raise "TODO"
  end

  it "the notification must have a source user" do
    raise "TODO"
  end

  it "the notificatio must notify a user" do
    raise "TODO"
  end

  it "notification must have an activity" do
    raise "TODO"
  end

  it "notification must be destroyed when the activity is destroyed" do
    raise "TODO"
  end

end
